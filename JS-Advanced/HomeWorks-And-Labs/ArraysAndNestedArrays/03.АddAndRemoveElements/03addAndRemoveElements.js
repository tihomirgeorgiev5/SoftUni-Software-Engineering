function solve(arr){ 
    let newArr = [];
    let iniNum = 1;
    for (let i = 0; i < arr.length; i++) {
        if(arr[i] === 'add') {
            newArr.push(iniNum + i)
            
        } else {
            if (condition) {
                
            } else {
                
            }
            newArr.pop(arr[i]);

            
        }
        
    }
        newArr.forEach(element => {
        console.log(Number(element))
    });

}
solve(['add', 
'add', 
'remove', 
'add', 
'add']))
